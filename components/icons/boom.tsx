import * as React from "react";
import type { SVGProps } from "react";
import { Sparkles } from "lucide-react";

const BoomIcon = (props: SVGProps<SVGSVGElement>) => <Sparkles {...props} />;
export default BoomIcon;
