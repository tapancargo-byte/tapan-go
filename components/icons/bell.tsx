import * as React from "react";
import type { SVGProps } from "react";
import { Bell } from "lucide-react";

const BellIcon = (props: SVGProps<SVGSVGElement>) => <Bell {...props} />;
export default BellIcon;
