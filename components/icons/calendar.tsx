import * as React from "react";
import type { SVGProps } from "react";
import { Calendar } from "lucide-react";

const CalendarIcon = (props: SVGProps<SVGSVGElement>) => <Calendar {...props} />;
export default CalendarIcon;
