import * as React from "react";
import type { SVGProps } from "react";
import { Plus } from "lucide-react";

const PlusIcon = (props: SVGProps<SVGSVGElement>) => <Plus {...props} />;
export default PlusIcon;
