import * as React from "react";
import type { SVGProps } from "react";
import { MapPin } from "lucide-react";

const MapPinIcon = (props: SVGProps<SVGSVGElement>) => <MapPin {...props} />;
export default MapPinIcon;
