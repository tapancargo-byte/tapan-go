import * as React from "react";
import type { SVGProps } from "react";
import { Circle } from "lucide-react";

const CircleIcon = (props: SVGProps<SVGSVGElement>) => <Circle {...props} />;
export default CircleIcon;
