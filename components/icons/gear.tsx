import * as React from "react";
import type { SVGProps } from "react";
import { Cog } from "lucide-react";

const GearIcon = (props: SVGProps<SVGSVGElement>) => <Cog {...props} />;
export default GearIcon;
