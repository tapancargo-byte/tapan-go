import * as React from "react";
import type { SVGProps } from "react";
import { Lock } from "lucide-react";

const LockIcon = (props: SVGProps<SVGSVGElement>) => <Lock {...props} />;
export default LockIcon;
