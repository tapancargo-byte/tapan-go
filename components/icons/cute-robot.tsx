import * as React from "react";
import type { SVGProps } from "react";
import { Bot } from "lucide-react";

const CuteRobotIcon = (props: SVGProps<SVGSVGElement>) => <Bot {...props} />;
export default CuteRobotIcon;
