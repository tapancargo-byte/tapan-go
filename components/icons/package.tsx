import * as React from "react";
import type { SVGProps } from "react";
import { Package } from "lucide-react";

const PackageIcon = (props: SVGProps<SVGSVGElement>) => <Package {...props} />;
export default PackageIcon;
