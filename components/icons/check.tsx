import * as React from "react";
import type { SVGProps } from "react";
import { Check } from "lucide-react";

const CheckIcon = (props: SVGProps<SVGSVGElement>) => <Check {...props} />;
export default CheckIcon;
