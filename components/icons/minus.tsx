import * as React from "react";
import type { SVGProps } from "react";
import { Minus } from "lucide-react";

const MinusIcon = (props: SVGProps<SVGSVGElement>) => <Minus {...props} />;
export default MinusIcon;
