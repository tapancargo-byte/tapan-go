import * as React from "react";
import type { SVGProps } from "react";
import { Eye } from "lucide-react";

const EyeIcon = (props: SVGProps<SVGSVGElement>) => <Eye {...props} />;
export default EyeIcon;
