import * as React from "react";
import type { SVGProps } from "react";
import { Shield } from "lucide-react";

const ShieldIcon = (props: SVGProps<SVGSVGElement>) => <Shield {...props} />;
export default ShieldIcon;
