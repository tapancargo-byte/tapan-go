import * as React from "react";
import type { SVGProps } from "react";
import { LayoutPanelLeft } from "lucide-react";

const LayoutLeftIcon = (props: SVGProps<SVGSVGElement>) => (
	<LayoutPanelLeft {...props} />
);
export default LayoutLeftIcon;
