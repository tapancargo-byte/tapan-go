import * as React from "react";
import type { SVGProps } from "react";
import { Brackets } from "lucide-react";

const BracketsIcon = (props: SVGProps<SVGSVGElement>) => <Brackets {...props} />;
export default BracketsIcon;
