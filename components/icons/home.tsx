import * as React from "react";
import type { SVGProps } from "react";
import { Home } from "lucide-react";

const HomeIcon = (props: SVGProps<SVGSVGElement>) => <Home {...props} />;
export default HomeIcon;
