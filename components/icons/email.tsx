import * as React from "react";
import type { SVGProps } from "react";
import { Mail } from "lucide-react";

const EmailIcon = (props: SVGProps<SVGSVGElement>) => <Mail {...props} />;
export default EmailIcon;
