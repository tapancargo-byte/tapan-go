import * as React from "react";
import type { SVGProps } from "react";
import { Cpu } from "lucide-react";

const ProcessorIcon = (props: SVGProps<SVGSVGElement>) => <Cpu {...props} />;
export default ProcessorIcon;
