import * as React from "react";
import type { SVGProps } from "react";
import { Atom } from "lucide-react";

const AtomIcon = (props: SVGProps<SVGSVGElement>) => <Atom {...props} />;
export default AtomIcon;
